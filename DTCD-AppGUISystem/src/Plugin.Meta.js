import { version } from './../package.json';

export default {
  version,
  name: 'AppGUISystem',
  type: 'core',
  title: 'Система основного интерфейса приложения',
  withDependencies: false,
  priority: 1,
};
